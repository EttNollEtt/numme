function smm = f(t, y)
omega = 9.914296342689682e+02;
smsa = [];
sms = zeros(length(t), 1);
for k = 1:14
    smsa = [];
    for a=1:length(t)
        smsa = [smsa; y(k)*sin(k*omega*t(a))];
        sms = [sms smsa];
end
smm = sms;
end
